apiVersion: v1
kind: PersistentVolume
metadata:
  name: seafile-testing-data-pv
  namespace: seafile-testing
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/kube/seafile-testing"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seafile-testing-data
  namespace: seafile-testing
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi	  
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: seafile-testing
  namespace: seafile-testing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: seafile
  template:
    metadata:
      labels:
        app: seafile
    spec:
      containers:
        - name: seafile
          image: azizxd/seafile_images
          ports:
            - containerPort: 3306
            - containerPort: 8000
            - containerPort: 8082
          securityContext:
            runAsUser: 1000
          volumeMounts:
            - name: seafile-testing-data
              mountPath: /opt/seafile/seafile-server-10.0.1
      volumes:
        - name: seafile-testing-data
          persistentVolumeClaim:
            claimName: seafile-testing-data